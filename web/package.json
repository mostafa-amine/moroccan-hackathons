{
  "name": "hackathons-morocco-web",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-data.mjs",
    "dev": "node scripts/sync-data.mjs && nuxt dev",
    "build": "node scripts/sync-data.mjs && nuxt build",
    "generate": "node scripts/sync-data.mjs && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "node scripts/sync-data.mjs && nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.4.6",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  }
}
